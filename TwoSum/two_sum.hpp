#ifndef __TWOSUM_H__
#define __TWOSUM_H__

#include <vector>

class Solution {
public:
    // The expected answer
    std::vector<int> TwoSum(std::vector<int>& nums, int target);
    // rearrange the list in order from smallest to largest
    std::vector<int> Rearrange(std::vector<int>& nums);
};

#endif // !__TWOSUM_H__
